<local:BackButtonPage
    x:Name="thisPage"
    x:Class="Quickstart_Sending_Local_Toast.ConversationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Quickstart_Sending_Local_Toast"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">
            
            <TextBlock
                Style="{StaticResource TitleTextBlockStyle}"
                Text="Conversation Page"/>

            <RichTextBlock Margin="0,12,0,0">
                <Paragraph>
                    In a real app, this would load the actual conversation.
                </Paragraph>
                <Paragraph />
                <Paragraph>Conversation ID:
                    <Run Text="{Binding ConversationId, ElementName=thisPage}"/>
                </Paragraph>
            </RichTextBlock>

        </StackPanel>
        
    </ScrollViewer>
</local:BackButtonPage>
